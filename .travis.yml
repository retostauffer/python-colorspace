language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
#  - "nightly"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install .
# command to run tests
make test
